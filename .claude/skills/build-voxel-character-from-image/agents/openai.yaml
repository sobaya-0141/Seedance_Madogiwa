interface:
  display_name: "Image to Voxel Character"
  short_description: "Build reviewed 360° voxel references and rigged GLBs"
  default_prompt: "Use $build-voxel-character-from-image to generate a four-view voxel turnaround, collect my feedback, and only then build the model."
